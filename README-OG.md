Generate PNG fallbacks for Open Graph images

Steps:

1. Install dependencies:

```bash
npm install
```

2. Run conversion script:

```bash
npm run convert-og
```

This will read SVG files in `assets/` and write PNG fallbacks next to them.
